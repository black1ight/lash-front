import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Відгуки'
}
export default async function ReviewsPage() {
	// const data = await ReviewsService.getByProductId()
	return <div></div>
}
